services:
  - type: web
    name: mi-plan-estudio
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: "" # Se debe configurar en el dashboard de Render
